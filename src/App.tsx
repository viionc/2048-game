import GameBoard from "./components/GameBoard";

function App() {
    return (
        <main className="flex justify-center items-center w-full flex-col gap-8">
            <GameBoard />
        </main>
    );
}

export default App;
